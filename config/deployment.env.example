# Fill these from the existing local artifacts. Startup never builds or downloads.
# make provider/image and make packages/gpu/image print immutable image IDs.
PROVIDER_IMAGE_REF=
WORKER_IMAGE_REF=
WORKER_GPU_UUID=
CHECKPOINT_DIRECTORY=
MOLFORMER_CACHE_DIRECTORY=
